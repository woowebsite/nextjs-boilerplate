# nextjs-boilerplate
Technical stack
- Nextjs
- typescript
- SWR
- Unitest: Jest, Enzyme


# Squash commit 
`
git checkout -b new_branch
git commit -m "commit 1"
git commit -m "commit 2"

git merge master --squash
`